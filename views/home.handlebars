


<div class="containerTasks">
   <div id="btn-nav" class="px-3 mb-5">
    <div>
  <a href="/addtasks" class="btn btn-add btn-lg btn-success shadow active" id="btn-add" role="button" aria-pressed="true">Adicionar Tarefa +</a>
  </div>

  <div id="btn-tasks" class="d-flex">
  <button class="btn btn-primary text-uppercase shadow px-3" id="btn-edi">Prioridades</button>
  <button class="btn btn-danger text-uppercase shadow ms-2 me-3 px-3" id="btn-urgency">Urgências</button>
  </div>
  </div>


 <div class="d-flex flex-wrap">
      {{#each show}}
        <div class="card" style="width: 18rem; ">
          <div class="card-body">
            <h5 class="card-title">{{this.title}}</h5>
            <div class="border mb-2"> </div>
            <h6 class="card-subtitle mb-2 text-muted">{{this.dataFormatada}}</h6>
            <p class="card-text">{{this.description}}</p>
            <div id="boxbtn">
              <div class="d-flex">
              <form action="/delete" method="POST">
                <input type="hidden" name="_id" id="_id" value="{{this._id}}" />
                <button class="delete" id="btn-del"><i
                    class="bi bi-trash"
                  ></i></button>
              </form>
              <form action="/edit" method="post">
                <input type="hidden" name="_id" id="_id" value="{{this._id}}" />
                <button class="edit" id="btn-edit"><i
                    class="bi bi-pencil"
                  ></i></button>
              </form>
              </div>
              <h6 class="card-title text-uppercase">{{this.urgency}}</h6>
            </div>
          </div>
        </div>
      {{/each}}
      
      
  </div>

 <div class="modal fade" id="edit-modal">
     <div class="modal-dialog">
         <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Prioridade:</h5>
                <button class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
               {{#each show}}
               {{#if priority}}
                <div class="card mb-3" style="width: 18rem; margin: 0 auto;">
          <div class="card-body">
            <h5 class="card-title">{{this.title}}</h5>
            <div class="border mb-2"> </div>
            <h6 class="card-subtitle mb-2 text-muted">{{this.dataFormatada}}</h6>
            <p class="card-text">{{this.description}}</p>
            <div id="boxbtn">
              <div class="d-flex">
              <form action="/delete" method="POST">
                <input type="hidden" name="_id" id="_id" value="{{this._id}}" />
                <button class="delete" id="btn-del"><i
                    class="bi bi-trash"
                  ></i></button>
              </form>
              <form action="/edit" method="post">
                <input type="hidden" name="_id" id="_id" value="{{this._id}}" />
                <button class="edit" id="btn-edit"><i
                    class="bi bi-pencil"
                  ></i></button>
              </form>
              </div>
              <h6 class="card-title text-uppercase">{{this.urgency}}</h6>
            </div>
          </div>
        </div>
            {{/if}}
               {{/each}}
            </div>
         </div>
     </div>
   </div>

 <div class="modal fade" id="urgency-modal">
     <div class="modal-dialog">
         <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Urgente:</h5>
                <button class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
               {{#each show}}
               {{#if prioritymax}}
             <div class="card mb-2" style="width: 18rem; margin: 0 auto">
          <div class="card-body">
            <h5 class="card-title">{{this.title}}</h5>
            <div class="border mb-2"> </div>
            <h6 class="card-subtitle mb-2 text-muted">{{this.dataFormatada}}</h6>
            <p class="card-text">{{this.description}}</p>
            <div id="boxbtn">
              <div class="d-flex">
              <form action="/delete" method="POST">
                <input type="hidden" name="_id" id="_id" value="{{this._id}}" />
                <button class="delete" id="btn-del"><i
                    class="bi bi-trash"
                  ></i></button>
              </form>
              <form action="/edit" method="post">
                <input type="hidden" name="_id" id="_id" value="{{this._id}}" />
                <button class="edit" id="btn-edit"><i
                    class="bi bi-pencil"
                  ></i></button>
              </form>
              </div>
              <h6 class="card-title text-uppercase">{{this.urgency}}</h6>
            </div>
          </div>
             </div>
            {{/if}}
               {{/each}}
            </div>
         </div>
     </div>
   </div>
  </div>  



